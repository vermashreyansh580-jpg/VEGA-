name: VEGA Final Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      
      # Flutter 3.24.0 sabse stable hai Flet ke liye
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.24.0'

      - name: Install Flet
        run: pip install flet

      # ---------------------------------------------------------
      # STEP 1: UI FILE (index.html)
      # ---------------------------------------------------------
      - name: 📝 Create assets/index.html
        run: |
          mkdir -p assets
          cat <<EOF > assets/index.html
          <!DOCTYPE html>
          <html lang="en">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <style>
                  body { 
                      background-color: black; 
                      display: flex; 
                      flex-direction: column;
                      justify-content: center; 
                      align-items: center; 
                      height: 100vh; 
                      margin: 0; 
                      overflow: hidden;
                      font-family: monospace;
                  }
                  .reactor {
                      width: 250px;
                      height: 250px;
                      border: 5px solid #00ff00;
                      border-radius: 50%;
                      box-shadow: 0 0 50px #00ff00;
                      display: flex;
                      justify-content: center;
                      align-items: center;
                      animation: pulse 2s infinite;
                  }
                  .core {
                      width: 150px;
                      height: 150px;
                      background: radial-gradient(circle, #fff 10%, #00ff00 90%);
                      border-radius: 50%;
                  }
                  h1 { color: #00ff00; margin-top: 30px; letter-spacing: 5px; }
                  @keyframes pulse { 0% { transform: scale(1); } 50% { transform: scale(1.05); box-shadow: 0 0 80px #00ff00; } 100% { transform: scale(1); } }
              </style>
          </head>
          <body>
              <div class="reactor"><div class="core"></div></div>
              <h1>VEGA ONLINE</h1>
          </body>
          </html>
          EOF

      # ---------------------------------------------------------
      # STEP 2: BRAIN (main.py)
      # ---------------------------------------------------------
      - name: 📝 Create main.py
        run: |
          cat <<EOF > main.py
          import flet as ft
          
          def main(page: ft.Page):
              page.title = "VEGA"
              page.bgcolor = "black"
              page.padding = 0
              
              # Load UI
              html_content = "<h1>Loading...</h1>"
              try:
                  with open("assets/index.html", "r") as f:
                      html_content = f.read()
              except:
                  pass

              webview = ft.WebView(html=html_content, expand=True)
              page.add(webview)
              
              # Dependencies Check (Taaki crash na ho)
              try:
                  import groq
                  import speech_recognition
                  print("✅ Brain Loaded")
              except ImportError as e:
                  print(f"❌ Error: {e}")

          ft.app(target=main, assets_dir="assets")
          EOF

      # ---------------------------------------------------------
      # STEP 3: BUILD APK (Corrected Flags)
      # ---------------------------------------------------------
      - name: 🚀 Build APK
        run: |
          # 🔥 FIX: Permissions ko keywords mein badal diya hai (camera, microphone, location)
          # Flet CLI SMS/Call permissions ko direct flag se support nahi karta,
          # lekin Microphone (Jarvis ke liye) yahan se mil jayega.
          
          yes | flet build apk \
            --project vega_app \
            --module-name main \
            --include-packages flet,groq,SpeechRecognition,gTTS,requests \
            --permissions camera,microphone,location \
            --description "Vega AI Assistant"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: VEGA-FINAL-APK
          path: build/app/outputs/flutter-apk/app-release.apk
